import Button from "../../componets/Button";

const RoomOperate = () => {
  return (
    <div className="w-full p-2 flex justify-end items-center">
      <Button buttonText="+ 新增房型 Add New" styledtype="confirm"></Button>
    </div>
  );
};

export default RoomOperate;
